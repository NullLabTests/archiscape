[project]
name = "archiscape"
version = "0.1.0"
description = "AI-powered codebase architecture reverse-engineering tool"
requires-python = ">=3.10"
dependencies = [
    "typer>=0.9",
    "rich>=13.0",
    "networkx>=3.0",
    "jinja2>=3.0",
]

[project.scripts]
archiscape = "archiscape.cli:app"
